# Municipal Intelligence CLI

CLI-first municipal intelligence pipeline for Indian cities.

The implementation plan lives in `plan.md`; execution status lives in `tracker.md`.

## Bootstrap

```bash
python3 -m pip install -e ".[dev]"
muni init
muni doctor
```

